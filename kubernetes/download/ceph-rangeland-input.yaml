apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ceph-rangeland-input
spec:
  storageClassName: cephfs
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 350G